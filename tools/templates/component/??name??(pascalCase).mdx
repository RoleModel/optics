import { Meta, Story, Canvas, Controls } from '@storybook/blocks'
import * as ??name??(pascalCase)Stories from './??name??(pascalCase).stories'

import { createAlert } from '../Alert/Alert.js'

<Meta of={??name??(pascalCase)Stories} />

# ??name??(pascalCase)

Brief overview description of how to use ??name??(pascalCase) classes

## Playground

<Canvas of={??name??(pascalCase)Stories.Default} />
<Controls of={??name??(pascalCase)Stories.Default} />

### Selective Imports

??name??(pascalCase) can be used as a standalone component, however, it does have a few dependencies. To see a full dependency list, see [Dependency Graph](?path=/docs/overview-selective-imports--docs#dependencies)

```css
// Depends on
@import '@rolemodel/optics/dist/scss/core/tokens';
@import '@rolemodel/optics/dist/scss/core/base';

// Component
@import '@rolemodel/optics/dist/scss/components/??name??(lowerCase)';
```

## Variations

### Default

Description of the default example

<Canvas of={??name??(pascalCase)Stories.Default} />

### Alternate

Description of alternate example

<Canvas of={??name??(pascalCase)Stories.Alternate} />

## ??name??(pascalCase) API

Styles are built on css variables scoped to the ??name??(lowerCase).

Here are the variables that can be customized.

{/* prettier-ignore-start */}
```css
--_op-
```
{/* prettier-ignore-end */}

## Customizing ??name??(pascalCase) styles

<div
  dangerouslySetInnerHTML={{
    __html: createAlert({
      title: 'Important!',
      description: 'These patterns represent how to customize the style of the ??name??(lowerCase) for your project.',
    }).outerHTML,
  }}
></div>

The ??name??(lowerCase) classes are built on a [sass placeholder selector](https://sass-lang.com/documentation/style-rules/placeholder-selectors)

This allows multiple classes to share the same behavior. You can modify all ??name??(lowerCase) behavior by overriding the `%??name??(lowerCase)-global` placeholder selector and setting any properties:

```css
%??name??(lowerCase)-global {
}
```

If you need to override the behavior of a particular ??name??(lowerCase) style, you can open the respective class and set or change properties

```css
.??name??(lowerCase)-alternate {
}
```

## New ??name??(pascalCase) Variations

<div
  dangerouslySetInnerHTML={{
    __html: createAlert({
      title: 'Important!',
      description: 'These patterns represent how to create new variations of the ??name??(pascalCase) for your project.',
    }).outerHTML,
  }}
></div>

Your application may need a custom ??name??(lowerCase). To add one, just follow this template:

```css
.??name??(lowerCase)-{name} {
  @extend %??name??(lowerCase)-global;
}
```
