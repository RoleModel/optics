import { Primary, ArgsTable, Canvas, Story } from "@storybook/addon-docs";

# Button

Button classes can be used on `button` or `a` html elements. They provide consistent and composable styling that should address most applications basic needs.

<!-- Either use <Stories /> to automatically render each story -->
<!-- Or manually render them with custom descriptions -->

## Default

`.btn` Provides a default outlined black text button. This would be commonly used as a cancel button in a form.

<Canvas withToolbar>
  <Story id='components-button--default' />
</Canvas>

## Primary

`.btn-primary` Provides a filled button using the primary color in your theme. This should be used as the primary action on a screen such as the login button on an account screen or the submit button on a form.

<Canvas withToolbar>
  <Story id='components-button--primary' />
</Canvas>

## Secondary

`.btn-secondary` Provides an outlined button using the primary color in your theme. This should be used as a secondary action on a screen such as the create account button on a login screen.

<Canvas withToolbar>
  <Story id='components-button--secondary' />
</Canvas>

## Delete

`.btn-delete` Provides a filled button using the red danger color. This should be used as a destructive action such as delete.

<Canvas withToolbar>
  <Story id='components-button--delete' />
</Canvas>

## Outline

`.btn--outline` This is a modifier which can be used with all button classes. It provides an outlined button that adapts it's color to all the btn variations.

<Canvas withToolbar>
  <Story id='components-button--outline' />
</Canvas>

## No Border

`.btn--no-border` This is a modifier which can be used with all button classes. It provides a button with no border, but the same sizing and adapts it's color to all the btn variations.

<Canvas withToolbar>
  <Story id='components-button--no-border' />
</Canvas>

## Disabled

`.btn--disabled` Should be used on `a` elements that need to be disabled. `button` elements can use the `disabled` attribute as well as this class. This makes the button opaque and changes the cursor to the `not allowed` icon.

<Canvas withToolbar>
  <Story id='components-button--disabled' />
</Canvas>

## Pill

`.btn--pill` Modifies the shape of any other buttons by adding a radius to the left and right.

<Canvas withToolbar>
  <Story id='components-button--pill' />
</Canvas>

## Icon

`.btn--icon` Modifies the shape of any other button class by adding a full radius to the button. This should only be used with an icon as the button text. You can also combine it with `.btn--no-border` for a borderless icon button.

<Canvas withToolbar>
  <Story id='components-button--icon' />
</Canvas>

## Size

`.btn--small`, `.btn--medium`, `.btn--large` (with medium being the default) modify the size of any other button class by changing the font and height to be smaller or larger.

<Canvas withToolbar>
  <Story id='components-button--size' />
</Canvas>

## Mobile Buttons

All button classes, regardless of the currently applied size modifier, will automatically adjust to the large button size on mobile screens ($breakpoint-small ~ 768px).
This is to ensure a large enough tap area on smaller devices and screens.

See https://uxmovement.com/mobile/optimal-size-and-spacing-for-mobile-buttons/

## Button API

The button styles are built on css variables scoped to the button.

Here is the full public API that the button uses.
```css
// Normal
--_rm-btn-background-color:
--_rm-btn-text-color:
--_rm-btn-border-color:

// Hover
--_rm-btn-background-hover-color:
--_rm-btn-text-hover-color:
--_rm-btn-border-hover-color:

// Outline
--_rm-btn-background-outline-color:
--_rm-btn-text-outline-color:
--_rm-btn-border-outline-color:

// Hover and Outline
--_rm-btn-background-hover-and-outline-color:
--_rm-btn-text-hover-and-outline-color:
--_rm-btn-border-hover-and-outline-color:

// No Border
--_rm-btn-background-no-border-color:
--_rm-btn-text-no-border-color:

// Hover and no Border
--_rm-btn-background-hover-and-no-border-color:
--_rm-btn-text-hover-and-no-border-color:

// General Properties (for size and height, the appropriate variable is used when using the size modifiers)
--_rm-btn-font-weight:
--_rm-btn-gap:
--_rm-btn-transition:
--_rm-btn-padding:
--_rm-btn-radius:

--_rm-btn-height-small:
--_rm-btn-height-medium:
--_rm-btn-height-large:

--_rm-font-small:
--_rm-font-medium:
--_rm-font-large:
```

## Overriding Button styles

The button classes are built on a sass placeholder selector https://sass-lang.com/documentation/style-rules/placeholder-selectors

This allows multiple button classes to share the same behavior. You can modify all buttons behavior by overriding the `%btn-global` placeholder selector and setting any of the **general properties** documented tokens:
```css
%btn-global {
  --_rm-btn-radius: var(--rm-radius-x-large);
}
```

If you need to override the color of a particular button style, you can open the respective class and change the **non-general properties** documented above
```css
// This will only affect the default button, but not primary, secondary, etc.
.btn {
  --_rm-btn-background-color: red;
  --_rm-btn-text-color: white;
  --_rm-btn-border-color: red;
}
```

## Custom Button

Your application may need a custom button. To add one:
1. Create a class like `.btn-redsky`
2. Extend `%btn-global`
3. Use the above documented API to define how it looks and feels for different states (e.g. hover, outline)

```css
.btn-redsky {
  @extend %btn-global;

  // Normal
  --_rm-btn-background-color: red;
  --_rm-btn-text-color: white;
  --_rm-btn-border-color: red;

  // Hover
  --_rm-btn-background-hover-color: darkred;
  --_rm-btn-text-hover-color: white;
  --_rm-btn-border-hover-color: darkred;

  // Outline
  --_rm-btn-background-outline-color: transparent;
  --_rm-btn-text-outline-color: red;
  --_rm-btn-border-outline-color: red;

  // Hover and Outline
  --_rm-btn-background-hover-and-outline-color: transparent;
  --_rm-btn-text-hover-and-outline-color: darkred;
  --_rm-btn-border-hover-and-outline-color: darkred;

  // No Border
  --_rm-btn-background-no-border-color: transparent;
  --_rm-btn-text-no-border-color: red;

  // Hover and No Border
  --_rm-btn-background-hover-and-no-border-color: transparent;
  --_rm-btn-text-hover-and-no-border-color: darkred;
}
```

## Playground

<Primary/>
<ArgsTable of='.' story='^'/>
